import { createTheme } from "@mui/material/styles";

export default function theme() {
  return createTheme({
    typography: {
      fontFamily: "'THICCCBOI', -apple-system,'sans-serif'",
    },
  });
}
